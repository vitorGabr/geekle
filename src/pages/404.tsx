import { Center } from "@chakra-ui/react";

export default function PageNotFound() {
    return <Center
        height="100vh"
        w={["100%"]}
        bgColor='black'
        color='white'
        fontSize={["2xl", "3xl", "4xl"]}
    >
        Page not found
    </Center>
}